[project]
name = "thnk"
version = "0.0.1"
authors = [
    { name="Jason Wilkes", email="notarealdeveloper@gmail.com" },
]
description = "A machine-learnable version of the python object system."
dependencies = [
    "jax",
    "nltk",
    "timestring",
    "is_instance",
]

[project.optional-dependencies]
develop = [
    "build",
    "pytest",
]

[project.urls]
source = "https://github.com/notarealdeveloper/think"
